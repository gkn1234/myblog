@import '../../styles/var.styl'

borderBottom()
  position: relative

  &::after
    content: ''
    position: absolute
    width: 100%
    bottom: 0
    left: 0
    height: 1px
    transform: scaleY(0.5)
    background-color: $border-color

.blog-card
  padding: 20px

  &:not(:last-child)
    border-bottom: 1px solid $theme-color

// 卡片头部部分
.blog-card-header
  padding-bottom: 20px
  borderBottom()

.blog-card-header-title
  font-size: 20px
  font-weight: 500
  margin-bottom: 5px

  a
    color: $word-color

    &:hover
      color: $theme-color

.blog-card-header-extra
  display: flex
  align-items: center
  flex-wrap: wrap
  font-size: 12px
  color: $word-tips-color

  span
    &:not(:last-child)
      margin-right: 20px

// 卡片主体部分
.blog-card-content
  padding: 20px 0
  min-height: 220px
  borderBottom()

.blog-card-content-img
  float: left
  width: 300px
  max-height: 180px
  margin-right: 10px
  clear: both

.blog-card-content-desc
  padding: 0
  overflow: visible

.blog-card-footer
  padding-top: 20px
  display: flex
  align-items: center

  span
    &:not(:last-child)
      margin-right: 20px